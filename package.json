{
  "name": "security-webhook-router",
  "version": "0.1.0",
  "private": true,
  "description": "Repository-level tooling for the security webhook router monorepo.",
  "scripts": {
    "env:check": "./scripts/validate_env_examples.sh",
    "test": "./scripts/run_component_tests.sh",
    "validate": "npm run env:check && npm test"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
